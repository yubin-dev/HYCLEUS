# Geliştirme / test / CI bağımlılıkları — runtime için requirements.txt'e bak.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=9.1
ruff>=0.16
mypy>=2.3

# Statik güvenlik analizi (bkz. SECURITY.md §6.8)
#
# `[toml]` ekstrası ZORUNLU: yapılandırma pyproject.toml içinde ve bandit
# TOML okumak için `tomli`ye ihtiyaç duyuyor. Çalıştırma:
#     bandit -c pyproject.toml -r CORE DB UI main.py
bandit[toml]>=1.9

# semgrep BURADA DEĞİL — requirements-security.txt içinde.
# Sebep: tekerleği ~57 MB ve test matrisinin iki ayağında da indirilmesi
# gerekmiyor. Statik analiz sonucu platformdan bağımsız; ayrı bir işte
# (ci.yml / `security`) yalnızca Linux'ta çalışıyor.

# CI özet betiğinin JUnit XML ayrıştırıcısı (B-019).
#
# `.github/scripts/test_summary.py` bunu KOŞULLU import ediyor: kurulu
# değilse stdlib ElementTree'ye düşüyor ve çalışmayı sürdürüyor. Betiğin
# görevi CI'ın ne durumda olduğunu söylemek; bir bağımlılık eksikliği
# yüzünden hiç konuşmaması, raporladığı sorundan kötü olurdu.
#
# Saf Python, kendi bağımlılığı yok, ~30 KB.
defusedxml>=0.7.1

# psutil BURADA DEĞİL — requirements.txt'e taşındı (B-103): artık yalnızca
# tests/test_scanner_backends.py'nin B-081 tutamaç/thread sızıntısı
# regresyon testi değil, CORE/worker_sizing.py de kullanıyor (üretim
# kodu). `-r requirements.txt` (yukarıda) zaten getiriyor, burada ikinci
# kez listelemek sürüm pininin iki yerde AYRIŞMASI riski taşırdı.

# Testler için per-test zaman aşımı (B-098) — bkz. pytest.ini'deki `timeout`.
#
# CI'ın kendi `timeout-minutes: 30`u ASILAN bir adımı 30 dakika SESSİZCE
# tüketip "cancelled" (kırmızı X DEĞİL) ile bitiriyor — hangi testin
# takıldığını gösteren hiçbir iz bırakmadan (ci.yml'nin kendi başındaki
# uyarı tam bunu anlatıyor: "Sessiz bir bekleme, açık bir başarısızlıktan
# her zaman kötüdür"). `pytest-timeout` her testi 120 saniyeyle sınırlıyor
# — yerel en yavaş test 18,76 sn (ölçüldü, `--durations=25`), Windows
# koşucusu daha yavaş olsa da 120 sn geniş bir pay bırakıyor. Windows'ta
# `SIGALRM` YOK, bu yüzden paket otomatik `thread` yöntemine düşüyor: testi
# zorla İPTAL EDEMEZ ama zaman aşımı anında TÜM thread'lerin yığın izini
# (stack trace) test çıktısına yazdırıyor — asıl kazanç bu, 30 dakikalık
# izsiz bir "cancelled" yerine TAKILAN satırı gösteren bir günlük.
pytest-timeout>=2.3
