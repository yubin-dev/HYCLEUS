PySide6
cryptography
# wmi pywin32'ye bağlıdır ve yalnızca Windows'ta kurulabilir.
# İşaretleyici olmadan CI'ın Ubuntu ayağı bağımlılık kurulumunda kırılır.
wmi; sys_platform == "win32"
pyotp
qrcode[pil]
argon2-cffi
# Sırlar (share_2, TOTP) OS anahtar kasasında tutulur:
# Windows Credential Manager / macOS Keychain / Linux Secret Service
keyring
requests
apscheduler<4
# KVKK envanter raporunun PDF çıktısı (CORE/inventory.py).
# Qt'nin QPdfWriter'ı da PDF üretebilirdi ve yeni bağımlılık gerektirmezdi,
# ama QApplication örneği ister: CORE bugün tamamen GUI'siz ve bir denetim
# raporunun üretimi ekran/olay döngüsüne bağlanmamalı (CLI, zamanlanmış
# görev, başsız CI). reportlab saf Python API'siyle başsız çalışır,
# BSD-3-Clause (proje MIT ile uyumlu) ve platypus çok sayfalı tabloda
# başlık satırını tekrarlar — denetim belgesi için gereken tam olarak bu.
reportlab
